---
# tasks for setting up web server

- name: setup docker networking for web
  docker_network:
    name: webnet

- name: create volume for postgres data
  docker_volume:
    name: webstatic

- name: run nginx server
  docker_container:
    name: nginx

